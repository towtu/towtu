name: Metrics
on:
  schedule: [{cron: "0 0 * * *"}]
  workflow_dispatch:
  push: {branches: ["master", "main"]}
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          user: towtu
          template: classic
          base: header, activity, community, repositories, metadata
          config_timezone: Asia/Manila
          
          # 🌟 FORCES WIDE / SIDE-BY-SIDE LAYOUT 🌟
          config_display: large
          
          # 🏆 GAMIFICATION: Cartoon Style Badges! 🏆
          plugin_achievements: yes
          plugin_achievements_display: compact
          plugin_achievements_secrets: yes
          plugin_achievements_threshold: C
          
          # 📊 GRAPHS: Coding Habits & Activity (Safely Configured!) 📊
          plugin_habits: yes
          plugin_habits_from: 200
          plugin_habits_days: 14
          plugin_habits_facts: no      # <--- Turning this off prevents the "Unexpected Error!"
          plugin_habits_charts: yes    # <--- This keeps the cool bar graphs!
          
          # 🧊 3D Isometric Calendar 🧊
          plugin_isocalendar: yes
          plugin_isocalendar_duration: full-year
          
          # 💻 Languages Progress Bars
          plugin_languages: yes
          plugin_languages_ignored: html, css
          plugin_languages_details: bytes-size, percentage
          plugin_languages_limit: 8
          
          # 📝 Lines of Code
          plugin_lines: yes
